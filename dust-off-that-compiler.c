#include <stdio.h>
/* The first exercise is basically an intro to C*/
int main(int argc, char *argv[]) {
    int distance = 100;
    printf("I've been running for %d meters.\n", distance);
    char name[5] = "Kevin";
    printf("Hi my name is %s", name);
    return 0;
}